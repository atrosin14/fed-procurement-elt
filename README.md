# ELT Workflow Using USA Spending data

This project uses Python to extract data from the USA Spending API and load it into a DuckDB database.

From there, dbt core handles data transformation, testing, and schema building under a medallion architecture.
