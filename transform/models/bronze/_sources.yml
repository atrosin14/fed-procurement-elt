version: 2

sources:
  - name: raw
    description: "Raw landed USA spending award data"
    schema: raw
    tables:
      - name: awards
        description: "One row per federal contract award, as returned by the API."
        loaded_at_field: "_loaded_at"
        freshness:
          warn_after: {count: 7, period: day}
          error_after: {count: 30, period: day}
